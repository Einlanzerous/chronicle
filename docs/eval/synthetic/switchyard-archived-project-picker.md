Small thing but it's biting me. Archived projects still show up in the project picker in Switchyard, so when I'm filing a ticket from the board I get the whole historical list including the two I archived months ago. I've actually filed into one of them by accident and didn't notice for a day. The list endpoint returns everything unfiltered, which I know because the vox-dictate salvage notes call that out about the same endpoint, so the fix is either filter on the server or filter in the picker. Server, probably, because whatever else is reading that endpoint has the same problem.
